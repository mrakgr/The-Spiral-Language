let g forall (q : *) t. (x:e) =
    inl f = function
        | a: b:q c: => 1
    q: 1 w: e: r: x
    inl rec f = function
        | f => f
        | qwe, asd => 2
    inl x = x
    inl y = 2
    {r with a = 1; b = x y; $a=1 without $a a e}
    ()
    234 : i32 -> _
    let h = real
        2 : i32 * `(2+3)
    (2 + 2)

let h {a b $r} =
    let rec g x = x
    and inl f x = x
    and inl g x = f x
    and inl f x = x
    and inl f x = x
    and inl h x = x