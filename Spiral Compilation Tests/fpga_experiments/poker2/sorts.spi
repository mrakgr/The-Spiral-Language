open corehls
open real_utils

inl merge_sort forall dim el. (inp : sa dim el) f : sa dim el =
    open sam
    inl ~inp = map id inp
    
    inp

inl main () =
    open sam
    inl ar = create : sa 8 card
    0i32