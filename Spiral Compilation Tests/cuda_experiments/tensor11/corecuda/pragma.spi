// Unrolls a loop.
inl unroll_push(x : i32) : () = !!!!PragmaUnrollPush(x)

// Unrolls a loop.
inl unroll_pop() : () = !!!!PragmaUnrollPop()

inl unroll body = 
    unroll_push 0
    body()
    unroll_pop()