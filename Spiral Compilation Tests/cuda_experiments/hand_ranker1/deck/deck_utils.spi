open corebase
open corecuda
open random_cuda

inl create () = deck ((1 <<< 52) - 1)
inl length (deck d) = intrinsic.popc_u64 d
inl index (deck d) i : bool = d &&& (1 <<< i) <> 0
inl toggle (deck d) i : deck = deck (d ^^^ (1 <<< i))

