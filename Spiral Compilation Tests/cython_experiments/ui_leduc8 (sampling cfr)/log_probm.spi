// Takes in a regular probability in the range (0,1] and converts it to a log one.
inl from prob = log_prob (log prob)
inl exp (log_prob x) = exp x
inl is_zero (log_prob x) = x = -inf
inl add (log_prob a) (log_prob b) = log_prob (a+b)