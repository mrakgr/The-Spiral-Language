nominal button = $"object"

instance toWidget button = fun (button x) => widget x
inl Create = 
    !!!!Import("kivy.uix.button")
    button $"kivy.uix.button.Button()"
instance text button = fun (v : string) (button x) => $"!x.text = !v"