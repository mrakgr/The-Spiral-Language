// Does `if type` work?

inl is_type_equal forall a b. (a : a) (b : b) =
    if type a = b then
        true
    else
        false

inl main() =
    inl ~_ = is_type_equal true 0i32
    inl ~_ = is_type_equal true false
    0i32