// Do the GADTs work?

union t y =
    | A :: t int
    | B :: t float
    | C :: t string

inl f forall y. (x : t y) =
    match x with
    | A =>
        inl x = x
        ()
    // | B => ()
    // | C => ()


inl main() : () = 
    inl x = A
    match x with
    | A => ()
    // | B => ()
    ()