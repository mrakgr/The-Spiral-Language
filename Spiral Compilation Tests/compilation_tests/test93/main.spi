// Do the GADTs work?

union t y =
    | A :: t int
    | B :: t float
    | C :: t string

inl f forall y. (x : t y) =
    match x with
    | A =>
        inl x = x
        ()
    | B =>
        inl x = x
        ()
    // | C => ()
    // match x with
    // | B =>
    //     inl x = x
    //     ()
    ()


// inl main() : () = 
//     inl x = A
//     match x with
//     | A => ()
//     // | B => ()
//     ()