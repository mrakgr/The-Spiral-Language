inl array_map f x =
        typecase `x with
        | i32 =>
            ()
        // array.init `(`(f ``t)) (array.length x) (fun i => 
        //     f (!!!!ArrayIndex(x,i))
        //     )

inl main () =
    inl x = array.init `i32 10 (fun i => i)
    array_map (fun x => x,x) x