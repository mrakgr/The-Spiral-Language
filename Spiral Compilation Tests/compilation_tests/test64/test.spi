// Is the type annotation getting propagated correctly into a record?

inl Nodes = 
    {
    action=fun p1 => 
        inl q = p1.observations
        ()
    } : { action : { observations : string} -> () }