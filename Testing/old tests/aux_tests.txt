let aux1: SpiralModule =
    {
    name="aux1"
    prerequisites=[queue; console]
    opens=[]
    description="Does the Queue module work?"
    code=
    """
open Console
open Queue
inl queue = create int64 1
inl rec dequeue' n =
    if n > 0 then dequeue queue |> writeline; dequeue' (n-1)
    else ()
Tuple.iter (enqueue queue) (Tuple.range (1,4))
dequeue' 2
Tuple.iter (enqueue queue) (Tuple.range (1,4))
Tuple.iter (enqueue queue) (Tuple.range (1,4))
dequeue' 2
dequeue' 4
dequeue' 4
    """
    }
